var lecture = "<%= @new_lecture %>";
if (lecture == "true") {
    $("#time_select_lecture").modal("hide");
    var eventData;
    eventData = {
        title: "Clase",
        url: '<%= "/groups/relations/#{@group.id}?lecture_id=#{@lecture.id}" %>',
        start: '<%= @lecture.date.strftime("%F") %>'
    };
    $("#error_success").stop().fadeOut(300, function() {
        $(this).html("Clase creada exitosamente").addClass("alert alert-success").removeClass('alert-danger').fadeIn(300, function() {
            $(this).delay(5000).fadeOut(300);
        });
    });
    $('#calendar').fullCalendar('renderEvent', eventData, true);
} else {
    $("#time_select_lecture").modal("hide");
    $("#error_success").stop().fadeOut(300, function() {
        $(this).html("No se pudeden crear clases en días pasados").addClass("alert alert-danger").removeClass('alert-success').fadeIn(300, function() {
            $(this).delay(5000).fadeOut(300);
        });
    });
}