<% provide(:title, "Generar Clases") %>

<div class="boton_der">
	<div class="top_buttons">
		<%= link_factory( :_path => group_path(@group), :_text => 'Volver al Grupo', :_class => 'btn btn-info', :_button => true ) %>
	</div>
	<h1>Generar Clases<span class="line"></span></h1>
</div>

<%= render 'calendars/form' %>