<% provide(:title, "Generar Clases") %>

<div class="boton_der">
	<div class="top_buttons">
		<%= link_to group_path(@group) do %>
			<p><button type="button" class="btn btn-info">Volver al Grupo</button></p>
		<% end %>
	</div>
	<h1>Generar Clases<span class="line"></span></h1>
</div>

<%= render 'calendars/form' %>